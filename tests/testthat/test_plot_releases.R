# qStates <- plot_releases("globalgov/qStates")
# 
# test_that("Plotting function visualises historical milestones/releases of a repository", {
#   expect_true(is.list(qStates))
#   expect_length(qStates, 9)
#   expect_named(qStates[1:3], c("data", "layers", "scales"))
# })
